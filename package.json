{
  "name": "figma-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for Figma design system maintenance with Claude",
  "type": "module",
  "main": "build/server.js",
  "bin": {
    "figma-mcp-server": "./build/server.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "prepare": "npm run build",
    "test": "tsx src/test-server.ts"
  },
  "keywords": [
    "mcp",
    "figma",
    "design-system",
    "claude"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "axios": "^1.6.0",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "tsx": "^4.7.0"
  }
}